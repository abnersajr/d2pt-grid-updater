{
  "name": "d2pt-grid-updater",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "postinstall": "bunx playwright install-deps && bunx playwright install chromium",
    "start": "bun run grid-updater.ts",
    "repair": "bun run grid-updater.ts --repair",
    "force": "bun run grid-updater.ts --force"
  },
  "devDependencies": {
    "@types/bun": "1.3.0"
  },
  "peerDependencies": {
    "typescript": "5.9.3"
  },
  "dependencies": {
    "playwright": "1.56.0"
  }
}